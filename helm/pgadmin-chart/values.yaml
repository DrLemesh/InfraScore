replicaCount: 1

image:
  repository: 941464113257.dkr.ecr.us-east-1.amazonaws.com/infrascore-pgadmin
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: ClusterIP
  port: 80
  targetPort: 80

# Environment variables for pgAdmin
env:
  PGADMIN_DEFAULT_EMAIL: "admin@infrascore.com" # Default, will be overridden by secrets
  PGADMIN_DEFAULT_PASSWORD: "admin" # Default, will be overridden by secrets

secrets:
  pgadmin_email: ""
  pgadmin_password: ""
